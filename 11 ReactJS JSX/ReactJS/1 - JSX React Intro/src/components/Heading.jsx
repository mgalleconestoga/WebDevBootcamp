import React from "react";              // Need this in order to be able to write JSX

function Heading() {
  // Convention is to capitalize the custom React components to differentiate from standard HTML elements
  // Functions become HTML Elements and are used in the same way
  return <h1>My Favourite Foods</h1>;
}

export default Heading;